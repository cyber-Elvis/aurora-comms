! Region A IOS-XRv 6.1.3: aurora-codex read/diagnostic role
! Change: CHG-AURORA-REG-A-CODEX-RBAC-001
!
! IOS-XRv 6.1.3 DOES NOT accept `task read all` or `task debug all`.
! The generator removes five reported but non-assignable metadata/reserved
! task IDs and emits the 77 accepted read/debug grants.
! Do not apply this file until the generated task block has replaced
! <GENERATED_READ_AND_DEBUG_TASK_LINES>.
!
! Apply identically to MEL-P, MEL-PE1, GEL-PE1, and ADL-PE1.
! Elvis enters and commits this block. Codex does not use labadmin or commit it.

configure
 taskgroup AURORA-CODEX-RO
  description Full read plus basic-services and debug with no write
  <GENERATED_READ_AND_DEBUG_TASK_LINES>
 !
 usergroup AURORA-CODEX-RO-USERS
  taskgroup AURORA-CODEX-RO
 !
 username aurora-codex
  group AURORA-CODEX-RO-USERS
  secret <RANDOM_ALPHANUMERIC_SECRET_FROM_VAULT>
 !
 root
 show configuration
 commit label AURORA_CODEX_RBAC
 end

! Apply large taskgroups in chunks of no more than 12-15 lines, then verify the
! complete taskgroup before committing. XR console pastes can silently drop lines.

! Evidence
show running-config taskgroup AURORA-CODEX-RO
show running-config usergroup AURORA-CODEX-RO-USERS
show running-config username aurora-codex
show user tasks
