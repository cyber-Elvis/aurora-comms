! Cisco IOS-XR placeholder template.
! Break-glass is full privilege. Automation/read accounts use explicit RBAC.

hostname <HOSTNAME>
domain name lab.aurora
ssh server v2
ssh server rate-limit 60

username admin
 group root-system
 secret <YOU_SET_THIS_ON_BOX>
!
taskgroup AURORA-CODEX-RO
 ! IOS-XR 6.1.3 has no `all` task ID. Generate one `task read <TASK-ID>`
 ! and one `task debug <TASK-ID>` for each assignable operational ID using:
 ! show aaa task supported
 ! The local generator excludes the five reported but rejected metadata/
 ! reserved IDs: cisco-support, disallowed, root-lr, root-system, universal.
 task execute basic-services
!
usergroup AURORA-CODEX-RO-USERS
 taskgroup AURORA-CODEX-RO
!
username aurora-codex
 group AURORA-CODEX-RO-USERS
 secret <RANDOM_ALPHANUMERIC_SECRET_FROM_VAULT>
!

! IOS-XRv 6.1.3 agent authentication is password-based. Retain the cleartext
! only in Ansible Vault; XR stores its platform-native hash.

! Restrict management sources with an ingress ACL on the management interface/VRF.
ipv4 access-list MGMT-SOURCES
 10 permit ipv4 host 192.168.137.1 any
 20 permit ipv4 host 192.168.137.2 any
 30 permit ipv4 host <PC2_GNS3_VM_OR_TAILSCALE_SOURCE> any
 40 permit ipv4 host <DO_HOST_OR_MGMT_SOURCE> any
 50 permit ipv4 host <OCI_HOST_OR_MGMT_SOURCE> any
 999 deny ipv4 any any log
!
